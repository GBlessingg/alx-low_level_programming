#include "main.h"
#include <stdio.h>

/**
* main - check the code
*
* Return: Always 0.
*/
void leet() {
  char s[]
  int i=0;
  while (s[i]!='\0')
  {
  if s[i] = 'L' || 'l' 
    {
      s[i] = 1;
    }
  if s[i] = 'E || 'e 
    {
      s[i] = 3;
    }
  if s[i] = 'T' || 't' 
    {
      s[i] = 7;
    }
}
  printf("\n The coded string: %s", s);
}
int main(void)
{
  char s[] = "Expect the best. Prepare for the worst. Capitalize on what comes.\n";
  char *p;
 
  p = leet(s);
  printf("%s", p);
  printf("%s", s);
  
  return 0;
  )
